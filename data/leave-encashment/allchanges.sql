CREATE TABLE HRNOC.HRIS_LEAVE_ENCASHMENT(
LE_ID INTEGER NOT NULL UNIQUE GENERATED BY DEFAULT AS IDENTITY,
LEAVE_ID INTEGER NOT NULL,
EMPLOYEE_ID DECIMAL(7),
TOTAL_ACCUMULATED_DAYS DECIMAL(7, 1),
REQUESTED_DAYS_TO_ENCASH DECIMAL(7, 1),
FISCAL_YEAR_ID INTEGER,
REMAINING_BALANCE DECIMAL(7, 1),
REQUESTED_DATE DATE,
MODIFIED_DATE DATE,
REMARKS VARCHAR(255)
);

ALTER TABLE HRIS_LEAVE_ENCASHMENT ADD (MONTH_NO INTEGER);

INSERT INTO HRNOC.HRIS_SALARY_TYPE VALUES ( (SELECT MAX(SALARY_TYPE_ID) + 1 from HRIS_SALARY_TYPE), 'Leave Encashment');


